{
  "private": true,
  "name": "gabber-sdks-ts",
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "clean": "pnpm -r run clean",
    "lint": "eslint --fix"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.0.0",
    "tslib": "^2.6.2"
  },
  "engines": {
    "node": ">=18"
  }
}